@AGENTS.md

## 글쓰기 지침 (2026-08-12 작성자 피드백 누적분 — 새 글·수정 시 항상 적용)

### 프레이밍 — 이 블로그는 취업시장용 포트폴리오다
- **결핍 고백 금지.** "우리 회사에는 ~이 없다" 같은 프레임은 단점 노출이다.
  같은 사실이라도 "내가 도입/설계를 고안해봤다"는 주도적 관점으로 쓴다.
- **대화 내용을 글에 옮기지 않는다.** 작성자가 지시·피드백으로 한 말(회사 상황,
  "비교하지 마" 같은 메타 발언)을 글 소재나 논지로 쓰지 않는다. 글은 처음 보는
  구독자를 향해 독립적으로 쓴다.
- 층위가 다른 도구(예: shadcn vs Storybook)를 "택일 비교" 구도로 쓰지 않는다.
- 인기 기술(shadcn, AI 도구 등)과 연결해 경쟁력 있는 소재로 만든다.
  **AI 도구에 어떻게 적용/강제하는가 섹션**을 넣는 것을 선호한다.

### 문체
- **제목은 간결하게.** "무엇이고, 왜 골랐고, 어떻게 쓰는가" 같은 구구절절한 나열
  금지 — "상태 관리를 Zustand로"처럼 짧게. 세부 구성은 본문에서만 드러낸다.
- 용어: "리포지토리/리포"가 아니라 **"레포지토리/레포"**로 쓴다.
- 한국어 평서체(~한다/~이다). 추상어·은유("씨앗", "지형", "합의의 축적",
  "조각", 테스트가 "싸다/비싸다", "양강 구도") 지양 — "조각"→"값",
  "싸다"→"쓰기 간단하다/몇 분이면 쓴다"처럼 구체어로. 반복 남발 금지.
- 명확하고 직관적인 단어 + 구체적 코드 예시. 정의 → 방법 → 예시 구조.
- 요청이 있으면 "직접 고민하고 쓴 듯한" 1인칭 고민체(미들급 프론트엔드 개발자
  관점)로 쓴다.
- **사견 최소화.** 원문 자료 기반 글에서는 개인 의견·해석을 과하게 붙이지 않는다.
  이유 설명은 원문에 있던 것만. 작성자가 준 문서는 규칙·예시를 최대한 원문대로
  살리고 문체만 블로그체로 변환한다(합니다체 → 한다체). 원문 오타는 고친다.

### 원문 문서를 글로 옮길 때
- 너무 많이 축약하지 않는다. 다른 글보다 살짝 길어져도 된다.
- 축약으로 빠지는 세부 설정들은 이유 없이 맨 하단 "그 외 추가 설정들" 섹션에
  코드 블록 하나로 묶고, 주석으로 어떤 설정인지만 표기한다.
- 원문 상단의 작성자 이름 등 개인정보, 미완성 섹션(제목만 있는 것)은 뺀다.

### 발행 위치와 날짜
- "N번째 글" = 날짜 오름차순 포지션. 앞뒤 글 날짜 사이 값으로 date를 정한다.
- 발행일은 백데이트 관행 — 글 내용과 시대착오가 없어야 한다
  (예: AI 도구·MCP 언급은 2025년 하반기 이후 날짜).

### 자료와 인용
- 웹서치 기반 글은 참조 링크 필수: 본문 인라인 링크 + 맨 하단 "참고:" 목록.
- **라이브러리·도구가 주제인 글은 공식 문서를 먼저 훑는다.** 블로그 요약글이
  아니라 공식 가이드 기준으로 표준 패턴(액션, 미들웨어, 권장 사용법 등)을
  빠짐없이 반영한다. 작성자가 하나씩 지적하게 만들지 말 것.
- 회사 코드를 인용할 땐 제품명·레포명·사내 고유명사를 익명화한다
  ("예약 서비스의 클라이언트 앱" 식). `npm run check`(sanitize) 차단 0건 필수.

### 이미지
- 메인 이미지: 작성자가 `assets/`에 올려주면 `public/thumbs/`로 옮기고
  frontmatter `thumbnail: "/thumbs/파일명"` 적용.
- 정사각형 로고류는 흰 배경 1200x630(1.91:1) 캔버스 중앙에 소형(~200px)으로
  합성해 가로형으로 만든다.
- 본문 이미지는 `![alt](/thumbs/파일명)` 마크다운. 관련 섹션 제목 바로 아래에.

### 작업 절차
- 글 추가/삭제/교체 시 `content/ideas.json`을 함께 갱신한다.
- 검증 루틴: `npm run check` → `npm run check:mdx` → 로컬(포트 3012) 렌더 200 확인.
- 작성자가 파일을 직접 수정한 부분은 보존한다. 수정 요청은 해당 부분만 고친다.
- **커밋·배포는 작성자가 명시적으로 요청할 때만.** 커밋 후 `gh run watch`로
  CI 통과까지 확인한다 (배포는 Vercel이 main push로 자동).

## 시리즈 기능 — UI만 제거된 상태 (2026-08-11)

글이 적어 시리즈 UI를 걷어냈다. **글이 쌓이면 시리즈 UI를 다시 확장할 것.**

- 데이터 레이어는 전부 살아있다: frontmatter `series`/`seriesOrder`/`seriesDescription`,
  `lib/posts.ts`의 `Series` 타입·`getSeriesList()`·`getSeries()`·`getSeriesNeighbors()`.
  글에 series frontmatter를 적는 것도 여전히 유효하다.
- 제거된 UI (되살릴 목록): 헤더 내비 "시리즈" 항목, `/series` 페이지,
  `/posts` 시리즈 필터(+`?series=` 딥링크용 useSearchParams/Suspense),
  글 상세 상단 시리즈 뱃지, 검색 다이얼로그 시리즈 표시,
  search-index.json의 series 필드, 커버·OG 이미지 라벨의 series 우선순위
  (현재는 tags[0]만 사용).
